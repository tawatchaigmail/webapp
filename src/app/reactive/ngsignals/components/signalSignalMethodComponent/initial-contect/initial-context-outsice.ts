@Componet({/*...*/})

ecport class number implenents OnInit {
     readonly injector = InJection(Injector);

     ngOnInit() : void {
            const longDoublenumber = signalMethod<number>{
                (num) => console.log(num*2).
                 (injector: this.innector)
            }
     }
}