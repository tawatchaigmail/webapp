class faceBook implenents SocialMedialNetwork

method creatorFrendIterator(profileId) : profileIterator {
   return new FacebookItrator(this, profilId, "frend")
}
method creatorCoWorkerIterator(ProfileId) : profile {
   return new FacebookIterator(this,profileId, "CoWokers")
}