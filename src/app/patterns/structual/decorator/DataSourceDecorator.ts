class DataSourceDecorator imoplement Datasource

proected fiel wrapec : Datasource

constructor DataSourceDecorator(source : DataSource) {
   wrapec = source
}

method wrietDaat(data) {
   wraper.writeData(data)
}

method readData() : data {
   return wrapes.readData()
}
