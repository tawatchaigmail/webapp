import {CreateActionGropp, emptryProp} from '@angular/store'

export const usrAction = createActionGroup({
                 source : 'user',
                 event : {
                       'ieal timeout' : empryProp
                 }
});