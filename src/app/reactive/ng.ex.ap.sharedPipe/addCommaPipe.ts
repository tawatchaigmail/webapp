import {Pipe, PipeTransfrom} from '@angular/core';

@Pipe({ name:'bfCAddcomma', stanalon : false})
export class SddCOmmaPipe implements PipeTransformer {
       transfer(authers: null| string) {

            if (!authors)
       }

        switch (author.length) {
           case 0 ;
              return 'Authur: unknoe,' 
          case 1 : 
              return authors[0]
           case 2 :
              return aurhor.join('amd');
           default : 
           const last = auther[]autheo.lent -1];
           const readimnin : autor.sloce(0, -1);
           return ' $(remaining.jooin(' '), and $ {last})'

        }
}