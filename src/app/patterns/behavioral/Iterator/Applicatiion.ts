class applicatin 

filed network : socialNetwork
fileld spammer : ScoiakNetwork

method config(){
    if working with Faebook
       this.network = new Facebook()
    if working with Linkein
       this.nework = new linkIn()
    this.spamer = new ScoialSpammer()
}

method senSpamInFrained(profile){
  iterator = network.createFranIterator(profile.getid())
  spammer.send(iterator, "vsery importan message")
}

method sendSpameCowoekerIteratr(profile){
  iterator = network.createCoworkIterator(profile.getId())
   spammer.send(Iteraor, "very importa mesage")
}