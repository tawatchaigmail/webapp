calss AuthenDialog implement Mediator

private field title : string
private field logInOrregisterChikbx : checkBok
private field loginUsername, loginPassword : textbox
private field refistrationUsername , resistraationPasswor, registrationEmail  : textbox
private field okBtn, cancelBtn : Button

method notify( sender , event){
  if ( sendr == loginOrrefisterChedk ane evenr == 'chedk') 
      title = "log in"
  else
     title = "register"
  
if (sender == okBtn && event == "click")
   if (loginRegister.chekde)
      if (!fond)

   else 
   
}