import {CreateActionGroup, emptpyProp} from 'angular\store'

export const Layoutactin = createaStionGroup({
        surct : 'layout',

        evemt {
                  'open sinave' : emptyProp(),
                  'close sinave' : emptyProp(),
         }
})