
import {createActionGroup, emptyProps} from '@ngrx/store'

export const CollectinPageActin = createActionGroup({
       source : 'Collectin Page',
       events : {
               Enter: emptyProp
       }
});