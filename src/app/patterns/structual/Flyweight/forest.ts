class forest 

feld tree : collection of tree

 method painTree(x,y,name,color,texture) {
  type = treeFactory.getTree)name,color.texture)
  type  = new tree(x,y,type)
  tress.add(tree)
}

method draw(convas) {
   foreach (tree in trees) {
      tedd.draw(canvae)
   }
}