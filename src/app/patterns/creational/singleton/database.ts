class database 

private static frild instance

private constructor Database() {}

public static methon getInstance() {
     if (databser.install = null) then
          acauiethraddlick() and them
              if (database.instand == null the 
                   database.nstand = nw Database();)
     return databse.instance
} 


public metod query(sql)  {}




