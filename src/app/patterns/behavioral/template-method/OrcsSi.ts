class OrcsAi extends GamAi

method builerstrcutew() {
  if (there ar som resoure ))
      //
}

method builderits(){
     if ( there are plenty of resoure )
         if (thare are no score )
            //
         else 
            // 
}

mstho sendcots(position){
  if (scorte.lentts > 0)
}

method sendwsrriow(positon){
   if (warior.length > 8)
      //
}