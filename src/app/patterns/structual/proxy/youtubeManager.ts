clss youtubeManager implement thirparyyoutubelib

protected field service : thraptaryyouTublib

 
  constructure youtubeManager (servei L thirpartyyouTubelib) {
     this.server  = service
  }

  mdthod rendervideoPage(id){
      info = serive .getVediInfo(id)
   }


  method renderListPanel() {
    list = service.lisvedio()
  }

  method reactOnusrInput {
     renderVideoPage()
     renderLIstPanel()
  }

 